"use client";

/**
 * Back-compat alias: every public page that imported SiteNav now renders THE
 * global site header (see SiteChrome). One bar everywhere, per product call.
 */
import { SiteHeader } from "./SiteChrome";

export default SiteHeader;
